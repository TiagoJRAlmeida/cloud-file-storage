# File system created locally using Kubernetes

Contrary to the other local version, which used `docker compose`, this version will use Kubernetes, but still locally (The cloud deployment is shown in the folder `GCP`).

